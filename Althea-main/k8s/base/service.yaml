apiVersion: v1
kind: Service
metadata:
  name: althea-backend
spec:
  selector:
    app: althea-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      name: http
  type: ClusterIP
