[pytest]
markers =
    unit: fast unit tests
    integration: integration tests
    security: security and authorization regression tests
    slow: long-running tests excluded from the default fast suite
    benchmark: benchmark tests excluded from the default fast suite
addopts = -m "not slow and not benchmark"
