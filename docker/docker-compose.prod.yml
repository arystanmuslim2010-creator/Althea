services:
  backend:
    environment:
      ALTHEA_ENV: production
      ALTHEA_ALLOW_SQLITE_IN_DEV: "false"
      ALTHEA_REQUIRE_POSTGRES: "true"
      ALTHEA_QUEUE_MODE: rq
      ALTHEA_WORKER_CONCURRENCY: "8"
      ALTHEA_PIPELINE_BATCH_SIZE: "50000"
  worker:
    deploy:
      replicas: 8
